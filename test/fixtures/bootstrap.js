import { createTest, insertTest, clean, scroller, wait } from './helper.js';
import assert from 'assert';

export const h = {
  createTest,
  insertTest,
  clean,
  scroller,
  wait
};

export { assert };
