import { createTest, insertTest, clean, scroller, wait } from './helper.js';
import { assert } from 'vitest';

export const h = {
  createTest,
  insertTest,
  clean,
  scroller,
  wait
};

export { assert };
